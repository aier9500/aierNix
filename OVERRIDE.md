# OVERRIDE

**Active override (2026-06-18):** Execute `action-plans/nix-structuring-rebuild.md` before any other ROADMAP items. The rebuild restructure is the current top priority and must complete (P0 through P3, merged to `main`) before new features or deferred items are picked up.

Design doctrine (design philosophy, minimal-root/rich-home split, feature-toggle convention, standalone HM rationale, quality gate, flatpak and Stylix policies) now lives in `DESIGN.md`.

This file will be cleared once P3 of the rebuild is complete.
